# Harbor Loop Development Notes

## V0.15.2 — Flatten the camera

远端车流太小。`PITCH` 是"从水平线往下转的角度"，所以**数值越大画面越平**（PI/2 是正俯视）—— 名字容易读反。实测四条赛道：

| pitch | 近/远大小比 | 赛道缩放 |
|---|---|---|
| 0.30（原值） | 1.86x | 0.845 |
| 0.60 | 1.64x | 0.942 |
| **0.90（新值）** | **1.46x** | **0.955** |
| 1.20 | 1.28x | 0.881 |

- **0.30 在两项指标上都是最差的**。角度越浅，靠近镜头的下半部分横向放大越厉害，所以它既让远端最小、又逼着 V0.15.1 的自动适配缩得最狠。改到 0.90 后远端明显变大，赛道整体反而比之前**大了 13%**（0.845 → 0.955）。
- **删掉了 `SCREEN_CY`**。它的值只在 PITCH=0.30 下成立，改角度就会把整个画面推出屏幕 —— V0.13.1 试 0.86 陡角失败、结论记成"远端糊掉"，很可能真实原因就是这个耦合，而不是角度本身。现在垂直定位交给 `fitCameraToPlane` 居中，`PITCH` 可以单独调。
- 赛道垂直居中在 HUD（到 y=56）和控制条（y=738）之间，实测 long-bay 落在 y 172..610，上下各留 116px。
- 还想更平就把 `PITCH` 往 1.20 调，注释里留了对照表；超过 1.20 透视就读不出来了，而且适配又开始吃尺寸。

## V0.15.1 — Fit every circuit on screen

真机上 Long Bay 有一段跑到屏幕外了。量了一下，四条赛道有三条溢出：

| 赛道 | 投影后 screen-x | 溢出 |
|---|---|---|
| long-bay | -15..425 | 左 15px、右 35px |
| switchback | -23..394 | 左 23px、右 4px |
| marina-sprint | 10..410 | 右 20px |
| grand-oval | 61..329 | 无 |

- **根因不是赛道画错了**。中心线确实都在设计框 `x 40..350` 内，但路面还要往两侧各铺 **39 单位**（`ROAD_HALF_WIDTH + 7` 的甲板阴影，再加 `ROAD_DEPTH` 的偏移），而透视投影在靠近镜头的下半部分把横向距离**放大到 1.31 倍**。`tracks/index.ts` 那句"都在 x 40..350 内"说的是中心线，把它当成"所以不会出屏"就是这次的错误来源 —— 注释已改。
- **修法：`fitCameraToPlane`**。每次 `setTrack` 时测量该赛道投影后的最外缘，需要时整体等比缩小。只缩不放，所以本来就装得下的 Grand Oval 一像素没动；等比缩放而不是单压 `FIT_X`，所以手调出来的取景比例得以保留，只是整体小一点点。long-bay 缩到 0.87，marina-sprint 0.96。
- 之前考虑过直接把 `FIT_X` 从 0.94 降到 0.80 —— 那会让四条赛道**全部**缩水 15%，而且横向压扁纵向不动，路面比例会变形。
- 新增回归测试：遍历 16 个模式（覆盖全部 4 条赛道），断言投影后的最外缘落在 `0..390 × 0..844` 内。这类问题只有一条断言拦得住，靠眼睛看必然会漏。

## V0.15.0 — Retune so the speed ramp is the thing you feel

上一版把唯一的难度定在 MASTER，结果是**游戏从自己的天花板开始**：起步 1.45× 速度、36 辆车，前几圈全在求生，串不起 Combo，也就永远看不到计分系统真正建立在什么机制上。

- **起步速度回到 125**（`playerSpeed` 1.45 → 1.0）。天花板一点没降 —— 10 次超车照样到 185 —— 但这 10 次现在够得着了。`playerSpeed` 同时乘在 Combo 的分段增益上，所以留在 1.0 也是让每次超车值一个看得见的 +6，而不是一个已经很大的数字上的零头。
- **车辆数 36 → 24**，AI 让路区域 0.36 → 0.75，同时并线 6 → 3，撞车无敌 0.75s → 1.15s。原来这几个值是配合"最高难度"设的，速度降下来后再留着就只是无谓的惩罚。
- **星级阈值的 1.3 倍加成撤掉**（`DIFFICULTY_STAR_SCALE` → 1.0）。那个加成是给"比基准更难的档位"定价的，现在档位就是基准；留着会变成用更少的分去够更高的线，正好和调整的目的相反。
- **档位标签 MASTER → STANDARD**。`Difficulty` 的键仍是 `'master'`（存档和榜单分区靠它，改了要做数据迁移），但显示名不能再声称这是最高难度。
- 起步 125 对最快 AI 117 —— 差距是故意留窄的。被动跑一局（完全不操作）实测：15 秒 7 次超车、速度 125 → 167，撞车会把 Combo 连同攒下的速度一起清空。
- 测试里的速度常量从 `MASTER_SCALE` 改名 `PLAYER_SCALE` 并归 1，另外新增两条断言：车辆数为 24，以及起步速度必须低于最快 AI —— 否则超车没有可赚的东西，整条曲线就塌了。

## V0.14.0 — Master only

- **删掉 NORMAL 和 TURBO，只留 MASTER**。游戏是围绕 Master 的车流密度调的，另外两档只是让人有机会选一个教得更少的版本。菜单上那排难度胶囊本来就是唯一的多选项控件，去掉之后菜单只剩「每日挑战 + 模式行」两个入口。
- `Difficulty` **收窄成 `'master'` 而不是删掉**。本地存档 key 和云端榜单分区都是 `模式:难度`，保留类型意味着不用动 `harbor_scores` 的结构和索引，以后要加回第二档也只是改数据。
- **星级阈值一个数都没变**：`DIFFICULTY_STAR_SCALE` 里 Master 一直是 ×1.3，现在只是没有别的档去对比它。玩家实际面对的三星线和改动前完全一致。
- **难度不再进解锁阶梯**，`difficultyUnlocked` / `difficultyUnlockCost` 一并删除，星星现在只解锁模式。单模式下 `nextUnlock()` 恒为空，所以菜单那行进度文案改成显示还差几颗星拿满，而不是一进游戏就宣布"全部已解锁"。
- **每日挑战两关都跑 Master**，第一关原本是 Normal。两关的落差现在完全由目标分承担（铜星线 → 金星线 ×1.45）。**第一关比以前难**，这是删掉 Normal 的直接代价。
- 菜单布局上移约 46px 填掉胶囊留下的空档；原胶囊位置改成一行 `MASTER · 36 车 · 最快 · AI 几乎不让路` 的说明文字 —— 不再是选项，但玩家仍然需要知道自己要面对什么。
- 测试里所有写死的速度常量原本是按 Normal（×1.0）标定的，现在统一按 `BASE_CRUISE × MASTER_SCALE` 表达，免得下次调难度又要挨个改数字。

## V0.13.4 — Grass texture

- 岸地和绿岛加可平铺的草地贴图：短笔触、混合角度、明暗两色。纯色绿读起来是剪纸，而岸地在画面里占的比例大到这件事有影响。

## V0.13.3 — Water ripple and per-panel concrete tone

- **水面重做**：原来是渐变加等间距横线，读起来像横格纸。改为可平铺的波纹贴图 —— 三组不同频率、不同相位的波峰叠加，加上稀疏的高光点。每组的周期数取整数，因此平铺无缝。
- **混凝土板块色差**：同一批浇筑的混凝土也不会完全一致；没有这个色差，接缝看起来像是在一整张纸上划出来的，而不是不同板块之间的缝。色差用确定性哈希生成，同一条赛道每次加载完全一致。

## V0.13.2 — Concrete slab seams and edge grime

- **路面横向接缝**：单一平涂读起来是"涂料"，真实混凝土是一块块浇筑的，板缝是让人认出材质的最强线索。接缝直接画在内外两条路沿之间，因此过弯时自动跟着弯。线宽三缝一重，避免看起来像机器印上去的。
- **边缘脏污**：靠近路沿的两条渐深带。路面在车轮走的地方最干净、在边缘最脏，这个渐变是"用过的路"和"新铺的路"之间的主要区别。
- 两者都在缓存静态层里，每帧零开销。

## V0.13.1 — Harbour scenery and a shallow tilt

按参考图补齐场景元素。

- **相机改为浅俯角**（PITCH 0.30、长焦）：近远缩放比控制在 2 倍以内 —— 有体积感，但远端车流仍然读得清。之前试的 0.86 陡角就是因为远端糊掉才撤掉的。
- **岩石岸线**：四角的深灰礁石，用带确定性扰动的不规则多边形生成，每块形状不同但每次运行一致；受光侧有亮顶。
- **港口建筑**：投影、侧墙、屋顶、亮面四层，配合起跑区的**黑白格地面**。
- **码头栈桥**：黄褐色桥面加横向木板，两侧栏杆更粗，水面有投影。Long Bay 内侧没有开阔水面，所以放在两侧水道靠泊船的位置 —— 强行架在绿岛上会很假。
- 所有场景元素都走同一个透视投影，并且全部在缓存的静态层里，每帧零开销。

## V0.13.0 — Daylight marina palette

按原作画面重做配色。

- **整体从夜间深色港湾改为日间码头**：浅混凝土路面、淡蓝灰水面、橄榄绿岸地。原来的暗色调让路面上的一切都是低对比。
- **路缘线改为连续的黄褐色边线**，取消红白相间的赛道路肩 —— 红白路肩读起来是赛车场，而这是一条码头公路。
- **AI 车改为蓝色**（原作里车流是青蓝色），红色母车因此成为画面上唯一的暖色，一眼就能找到。
- **水面新增停泊的白色游艇**（船体、甲板、驾驶舱、水面投影），放在赛道两侧的水道里。
- 车道深浅对比加大 —— 浅色混凝土上需要比深色沥青上更大的色差才读得出来。

## V0.12.1 — Back to top-down, lanes shaded instead of dashed

- **相机切回正交俯视**。倾斜视角在竖屏手机上，远端的车流小到读不出来 —— 而读车流是这个游戏的全部。透视代码保留（`camera.ts` 里一个 `PERSPECTIVE` 常量），因为它逼出来的填充式路面渲染本身比原来的描边更好，而且想再打开只需要改一个常量。
- **车道改为一深一浅交替填充，取消虚线分道线**。实心色带能一眼读出五条独立通道；虚线读起来是纹理，眼睛还得自己判断车道边界在哪。

## V0.12.0 — Real perspective, in Canvas 2D

画面"不够立体"的根本解决：**给地平面加一个真正的 3D 相机**，不换引擎、不用 WebGL。

- 新增 `render/camera.ts`：设计平面被一个抬高、俯角 49°、有焦距的相机投影。远端真正收窄、远处的东西真正变小 —— 这是透视，不是斜切。**这正是当年街机赛车的做法**：点在 JS 里投影，结果用 Canvas 2D 画出来。
- 相机固定不动，所以赛道的投影只算一次，仍然待在缓存的静态层里；每帧只投影车辆。
- **路面改为填充而非描边**：有了透视之后，等宽描边是错的（路必须随距离变窄）。新增 `fillRibbon()` / `fillBands()`，路面、护墙、路沿、分道线全部由两条投影后的边界填充而成。
- 车辆按投影后的深度缩放，朝向按投影后重新计算（透视会旋转方向），并**按深度从远到近排序绘制**，否则近处的车会被远处的挡住。
- 绿岛变成投影四边形（远边比近边窄），树、遮阳伞、浮标按各自位置的深度缩放。
- 尾迹、速度线、带电车道、粒子、爆炸全部走同一个投影。
- 为什么不用 WebGL：一张画布只能有 2d 或 webgl 一个上下文，切过去意味着整个 2D 场景要挪到离屏画布再每帧上传成纹理 —— 这个用法在微信真机上没验证过，而纯 Canvas 2D 是小游戏最没有兼容性风险的能力。

## V0.11.4 — Trail rebuilt as an additive streak

- 之前用半透明车身副本做拖影，在灰色路面上会糊成一团 —— 低透明度的细节图读不出"运动"。
- 改为沿实际行驶轨迹绘制**渐变实心尾带**：尾端宽而饱和，靠近车身收窄。
- 内芯改用**加色混合**（`globalCompositeOperation = 'lighter'`）：拖影点亮下方的路面而不是覆盖它，这是"发光"和"涂色"的区别，也是它能在深色沥青上一眼读出的原因。两层内芯（橙 + 暖白）。
- 尾带之上仍保留 1~3 个实心残影，让尾巴里还有"车"。

## V0.11.3 — Speed-driven afterimage

- 母车残影改为**三个维度都随速度增长**：残影数量 2→6、间距 2→4 帧、每个残影随拖远而膨胀最多 22%。巡航时完全不出现，全速时拖出一条明显变长变宽的渐隐尾迹。
- 由远及近绘制，最近的一个压在旧的上面，读起来是模糊拖影而不是一排复制品。
- 轨迹缓冲从 12 帧加长到 26 帧，够最长的拖影取样。

## V0.11.2 — Pre-rendered vehicle art and road grain

- **车辆改为预渲染精灵**：启动时以 10 倍尺寸把车画到离屏画布，运行时每辆车只需一次 `drawImage`。这反转了成本结构 —— 插画层次可以任意堆（车轮、车身渐变、车头下压、车厢、玻璃渐变加反光条纹、赛车条纹、钣金分缝、前后灯、受光边缘高光），而回放比原来十几次路径填充**更快**。放大绘制再缩小是边缘平滑的来源。
- **不使用外部图片资源**：小游戏包体不增加，也不需要美术交付。
- **路面沥青颗粒**：一次性生成可平铺的噪点贴图，沿赛道以 pattern 描边，颗粒只会落在路面上。纯色是"示意图"，有颗粒才是"路面"。
- 精灵和贴图在离屏画布不可用时自动退回原来的图元绘制。

## V0.11.1 — Lighting and depth

画面"不够立体"的一轮修正。**没有换引擎** —— 这是光影问题不是渲染管线问题。

- 新增 `render/light.ts`：全场景统一一个光照方向（右下投影）。所有形体按同一方向偏移，这正是俯视画面读起来是"实体"而不是"印刷图案"的原因。光照在屏幕空间，所以车过弯时影子方向不会跟着车转。
- **车辆**：投影（硬边偏移剪影，比模糊更清晰也更便宜，同时去掉了 `shadowBlur`）→ 侧面（沿光照方向偏移，产生厚度）→ 顶面 → 受光边缘的轮廓高光 → 车厢再抬升一层 → 挡风玻璃高光。
- **赛道**：变成浮在水面上的高架平台 —— 水面投影、侧壁、路面，外加受光侧的边缘高光和护栏内侧的环境光遮蔽。
- **绿岛**：同样的三层（投影 / 侧壁 / 顶面）。
- **暗角**：一次径向渐变，对"这是一个被照亮的场景"这件事的贡献比任何单项改动都大。
- 以上除车辆外全部在缓存的静态层里，每帧零开销。

## V0.11.0 — Playtest feedback: speed curve, five lanes, countdown, single mode

依据实际试玩反馈的一轮调整。

- **速度曲线重做**：从"每 10 辆跳一档"改为**每超一辆都加速**，且递减：前 10 辆每辆 +6，11–25 每辆 +3，26–50 每辆 +1.5，之后每辆 +0.55。巡航上限从 620 降到 **380**，油门固定再加 60。原来的阶梯让前期毫无手感、后期一辆车突然加 60，现在加速感集中在开局，中后期变成缓慢爬升。
- **赛道从 6 车道改为 5 车道**，车道间距相应从 8.2 加宽到 9.9，路面宽度不变。
- **开局 3-2-1 倒计时**：倒计时期间世界完全静止（车流、玩家、计时、油门输入全部冻结），新增 `countdown.ts`。
- **只开放 Combo Racers 一个模式**。其余 15 个模式代码和测试都保留，通过 `RELEASED_MODE_IDS` 控制放出。难度解锁成本改为按已发布模式数量换算，否则单模式下 MASTER 永远拿不到（一个模式最多 9 颗星）。
- **Combo Racers 改为纯计分**：分数是本局最大连击数，取消 40 连击的过关条件。撞车断连击但不清除记录。
- **HUD 只显示最大连击数**（BEST），当前连击数改为每次超车在母车上弹出一个数字后立即消失。
- **母车残影**：按速度强度绘制 3 个渐隐的历史位置，读起来像动态模糊但不需要真的模糊。
- **音效**：发动机增加低八度正弦"车身"层（略微失谐产生拍频）；新增**过弯胎噪**层，由赛道曲率和速度推出的过弯负荷驱动，入弯快起、出弯慢收；撞车音重做为低频下沉 + 金属振铃 + 摩擦噪声三层，噪声衰减拉长。

## V0.10.5 — Onboarding and daily streak

- 新增**首次引导**：新玩家此前面对 16 个模式和一块没有任何说明的屏幕，完全不知道底部是方向盘。现在第一局会在按键上方显示提示，用过哪个控件哪条提示就消失，两个都用过（或 12 秒后）永久关闭。提示带深色底衬 —— 直接叠在路面上完全看不清。
- 新增**连续天数**：每天第一局累计，断一天归零，显示在菜单头部。"明天再来" 的拉力远弱于 "明天再来，否则七天连续就断了"。
- 依据：行业数据显示社交渠道用户占爆款的 30-50%，且留存高出大盘 20%，而收益公式是「首次激活 × 留存 × 变现」—— 首次激活此前完全空白。

## V0.10.4 — Revive-by-share, share card, sound design

- 新增**分享复活**：撞毁或超时后每局给一次复活机会，条件是分享。复活续上同一局（分数保留、2.5 秒无敌脱身、限时模式补 15 秒），达成目标的局不提供复活。分享按钮此前是纯利他行为，没人会点；复活把它变成利己行为，这才是真正的裂变引擎。
  - 诚实说明：小游戏的 `shareAppMessage` 只打开选择器，不会告诉你用户是否真的发送了，要校验必须用 shareTicket 加服务端。所以分享触发后无论如何都给复活。
- 新增**分享图**：按微信裁剪的 5:4 比例离屏绘制成绩卡（品牌、模式、难度、大号分数、星级、本局赛道的轮廓速写），通过 `toTempFilePathSync` 作为 `imageUrl` 传给分享。带图的卡片才会被点开。
- 音效补齐：菜单点击 / 确认 / 拒绝三种 UI 音，过关号角，复活音效。新增**静音开关**（菜单头部），偏好持久化 —— 小游戏经常在公共场合玩，静音必须一键可达。
- `app.ts` 和 `share.ts` 出现互相导入，改为通过 `setShareContext()` 单向传递分享文案所需数据。
- 修复菜单里两位数解锁星数与星星图标重叠（改为按实测文本宽度定位）。

## V0.10.3 — Daily challenge, seeded traffic, cloud leaderboard

- 新增**每日挑战**：每天固定一个模式和一份车流种子，全服完全一致，因此分数可以直接比较。菜单顶部有独立入口，不受星级解锁限制 —— 它是每天所有人唯一的共同话题，锁住就没意义了。
- 新增种子随机数 `rng.ts`（mulberry32）。所有影响模拟的随机（AI 决策、变道方向、初始车距、Last Man 自爆目标、Ghost Lane 带电车道）都改走它；音频噪声、粒子散布、震动角度是纯装饰，保持 `Math.random`。
- 每日挑战分两关：**第一关**普通难度、目标为该模式的铜星线，几乎人人能过；**第二关**最高难度、目标为金星线的 1.45 倍，设计成绝大多数人过不去。过第一关直接进第二关，中间不插结算页 —— 断崖感来自两关的落差，中间停一下就泄了。
- 修复一个真 bug：帧间隔 `lastTime` 跨界面延续，在菜单停留后开局第一帧的 dt 会被钳到 50ms 上限。这既是开局的一次可见卡顿，也让同种子的两局在第一帧就分叉。新增 `clock.ts` 接管帧间隔，开局重置为标称 1/60。
- 新增**云开发排行榜**：`cloudfunctions/` 下 `submitScore` / `topScores` 两个云函数 + `harbor_scores` 集合。openid 取自微信上下文而非客户端参数，只在成绩变好时写入。结算页排行榜卡片可在「好友 / 全服」切换；每日挑战写入按日期分区的独立榜单。
- 云环境未配置时全部降级，游戏照常运行。
- 新增 `tests/daily.test.mjs`（22 项）：计划是日期的纯函数、Time Attack 被排除在池外、同种子重放一致、两局车流完全相同、第一帧确定、过第一关直接进第二关、每日成绩不覆盖模式最好成绩。

## V0.10.2 — Cached track layer, game feel

- **性能**：先测量再优化。canvas 阴影（36 车 0.22ms vs 无阴影 0.24ms）和 O(n²) 的 AI 查询都不是瓶颈；真正的开销是 `drawCurbs` 每帧对每个路段单独 `stroke`（长赛道约 2000 次调用，实测 1.0ms），画的还是永远不变的东西。改为每条赛道渲染一次到离屏画布再 blit，实测降到不可测量。
- `platform.ts` 新增 `withRenderTarget()`，让渲染函数不用改签名就能指向离屏画布；`createOffscreenCanvas()` 遵循微信"第一次 `wx.createCanvas()` 是显示画布、之后是离屏"的规则。没有离屏画布时退回逐帧实时绘制。
- 新增粒子系统（固定 160 个对象池，游戏中零分配）：撞车、摧毁、超车升档、Close Call 各有不同的爆发参数。
- 新增 `feel.ts`：hit-stop（撞车冻帧 75ms、摧毁 45ms）和屏幕震动（沿撞击轴向，约 0.18 秒衰减）。震动只位移世界，不动 HUD。
- 新增速度反馈 `speedLines.ts`：车后拖影 + 高速时相邻车道的流线，强度由"当前速度超出巡航速度多少"驱动，巡航时完全不可见。
- 新增 **Close Call**：贴着车超过去（车道距离 ≤1.25、路径距离 ≤34）会触发闪光、粒子、音效，并给 0.55 秒的加速提升 —— 冒险的走线变得实际更快，而不只是好看。
- 新增撞车音效（带通滤波噪声爆发 + 低频撞击体）和 Close Call 音效。撞车此前完全静音，而研究显示无音效会让打击感主观强度下降 50-70%。
- 测试增至 88 项，覆盖 Close Call 触发、撞车粒子/冻帧/震动、hit-stop 期间世界静止、效果衰减、新一局清空残留效果、离屏画布确实被创建。

## V0.10.1 — Star ladder, four circuits

- 新增星级进度系统：每个（模式 × 难度）按最好成绩对照阈值给 0~3 星；开局只开放 3 个模式，其余按 3/6/10/14/19/24/30/36/43/50/58/66/75 星逐个解锁，TURBO 需 6 星、MASTER 需 20 星。每级成本都远低于"已解锁模式 × 9"的上限，因此永远不会卡死。
- 菜单显示总星数、下一个解锁目标和差距；锁定行显示锁和价格，点击会给出提示而不是无反应。结算页显示本次星级和下一颗星所需分数。
- 赛道从写死一条改为可切换：新增 `src/tracks/`，用 `PathBuilder`（直线 + 圆弧）构造，`setTrack()` 重建全部派生几何（弧长表、六条车道路径、分道线、内外沿）。
- 新增 3 条赛道：GRAND OVAL（体育场，圈最短最快）、SWITCHBACK（9 段短直道 + 密集发夹）、MARINA SPRINT（中等圈长）。每个模式绑定一条赛道。
- 景观装饰（草坪岛、树、遮阳伞、浮标）改为每条赛道单独定义，原来是按 Long Bay 硬编码的，换赛道后会压在路面上。
- 修复 SWITCHBACK 最后一段直道转入回程时的 90° 硬折角，改为圆弧过渡。

## V0.10.0 — 16 modes, mode select, friend leaderboard

- 玩法从单一模式扩展为 16 个模式 × 3 个难度；难度只缩放速度（1.0 / 1.25 / 1.5），玩家和车流同步。
- 前 7 个模式（Speed Monkey / Combo Racers / Sunday Drivers / Fireball Frenzy / Death Race / In The Zone / Hot Rods）依据 PixelJunk Racers 的公开资料实现；其余 9 个公开资料没有记载规则，为原创，菜单和 README 都标注了来源。
- 新增 `ModeDefinition` 契约：模式通过 `setup / update / onOvertake / onContact / onDestroy / onCrash / cleared / failed` 钩子改写规则。碰撞几何仍在 `scoring.ts`，但"碰撞意味着什么"（撞毁 / 摧毁 / 无视）改由当前模式决定。
- 新增界面状态机 `app.ts`：菜单 / 比赛 / 结算。输入按界面路由，比赛中右上角有暂停返回键，键盘 `R` 重开、`Esc` 回菜单。
- 车辆增加 `alive / wreck / hasZone / zoneFill`，玩家增加 `fireball / heat / travelled`，支持摧毁、尾流区域、火球和过热。
- 新增全屏效果模块 `effects.ts`：Ghost Lane 的带电车道和 Blackout 的熄灯由模式驱动，渲染层只读取。
- 每个（模式 × 难度）单独存最好成绩；全部成绩之和作为生涯积分，通过 `wx.setUserCloudStorage` 上传。
- 新增开放数据域子包 `openDataContext/`，独立 JS 环境渲染好友排行榜（含头像、昵称、自己高亮），主上下文把共享画布贴到结算页。该目录不经 esbuild 打包。
- 结算页和下拉菜单都挂了分享，卡片标题带模式、难度和成绩，`query` 让点开的人直接进同一模式。
- 微信之外（浏览器预览）好友榜和分享自动降级为空实现，不影响运行。
- 难度从"只缩放速度"改为五维档案（`DifficultyProfile`）：玩家速度、车流速度、车辆数（24/30/36）、AI 让路区域缩放、同时并线的 AI 数量、撞车后无敌时间。Normal 原来偏简单，现在车辆数从 18 提到 24 并收紧了 AI 让路区域。
- 车流改为按需生成（`buildBlueprints(count)`），用黄金比例分布位置，任意车辆数都不扎堆；原来是写死的 18 辆。
- 菜单和结算页视觉重做：新增 `render/ui.ts`（硬投影 + 粗描边 + 平涂的卡片、按钮、胶囊标签），米白卡片配深色底、单一暖色主 CTA。比赛画面配色不变。
- 测试拆出共享 harness，新增 `tests/modes.test.mjs`：16 个模式全部跑到终局并断言分数有效，另有难度缩放、Death Race 摧毁、Hot Rods 过热、In The Zone 标记数、菜单点击、成绩存档等断言。两个测试文件共 91 项。

## V0.9.6 — TypeScript 重构与模块拆分

**玩法没有任何改动**，这一版只动工程结构，为后续加排行榜、结算页、皮肤等内容做准备。

- 源码改为 TypeScript，放在 `src/`，由 esbuild 打包成根目录的 `game.js`。
- 原来的单文件 1388 行拆成 16 个模块：平台适配、配置、赛道几何、音频、状态、玩家、AI、计分、输入、控件布局、渲染（primitives / scenery / road / vehicles / hud）、入口。
- `game.js` 变成生成文件，文件头有 GENERATED 标记；改代码请改 `src/`，然后 `npm run build`。仍然提交到仓库，这样微信开发者工具导入后不需要先构建。
- `tsc --noEmit` 开启 strict、noUnusedLocals、noUnusedParameters 全量检查。
- 音频不再直接读游戏状态：`audio.update(dt, snapshot)` 接收一个快照，因此 audio 和 state 互不依赖，可以各自单独测试。
- 新增 `tests/controls.test.mjs`：在 Node 的 vm 里加载打包产物并模拟微信触摸事件，26 项断言覆盖油门、多点触控、滑动释放、触摸取消、车道按键和 30 秒带车流的完整模拟。`npm run verify` 一次跑类型检查和测试。
- `project.config.json` 的 `packOptions.ignore` 排除 `node_modules`、`src`、`scripts`、`tests` 等，避免开发文件被打进小游戏包体。
- 新增 `.gitignore`，忽略 `node_modules` 和微信开发者工具生成的 `project.private.config.json`。

## V0.9.5 — On-screen controls and mobile throttle

- 后续版本统一从 V0.9.5 继续开发。
- 屏幕底部加入可见控制条：两个变道键和一个油门键，替代原来完全不可见的操作方式。
- 手机端此前没有油门，速度只能靠每十辆自动升档；现在触屏玩法和键盘一致。
- 按键区位于 y=738~810 的设计坐标；赛道最外层描边到 y=733 为止，因此不遮挡路面和车辆，下方也留出 iPhone 手势区。
- 触控改为按 `identifier` 逐指跟踪，支持一指按住油门、另一指点变道。
- 手指滑出油门键会松开油门，滑回来重新加速；`onTouchCancel` 和小游戏切后台也会释放所有触点，避免卡在持续加速。
- 只在 `wx.onTouchStart` 不存在时才绑定 DOM pointer 事件，避免开发者工具里同一次点击被处理两次、一下变两条道。
- 保留赛道区域的左右半屏点击变道，老玩法不受影响。
- 变道键按下有 0.14 秒高亮反馈，油门键在按住期间保持高亮。

## V0.9.4 — Ten-pass speed tiers and engine rebuild

- 后续版本统一从 V0.9.4 继续开发。
- 取消“每超过一辆就永久增加一点速度”；普通超车只增加 Combo。
- 每累计 10 辆进入新速度档，并使用约 0.72 秒的强加速阶段追向新目标。
- 速度档改为：x0 125/175、x10 180/240、x20 240/310、x30 305/380、x50 420/505、x70 515/600、x100+ 620/705。
- x70 的巡航和油门速度较 V0.9.3 大幅提高，解决超过 70 辆后仍感觉很慢的问题。
- 普通油门加速度从 48 提高到 112；跨十辆时短时使用 165 的档位加速度。
- 发动机重构为三层振荡器加滤波机械噪声，降低单音合成器感。
- 发动机基础转速与速度档同步，每十辆提高一次；油门只控制档内升转。
- 新增十辆节点的轻量机械升档声和 Combo 数字脉冲。
- 高速 AI 前方安全区上限从 145 扩展到 265，并增加后方保护距离。
- 新增逐帧扫掠碰撞检测，避免 600+ 速度或低帧率时穿过同车道 AI。
- 保留 V0.9.1 的内外车道真实线速度修复。

## V0.9.3 — Throttle prototype and 10-pass RPM tiers

- 后续版本统一从 V0.9.3 继续开发；包含 V0.9.1 的内外车道真实速度修复和 V0.9.2 的声音重调。
- 开局巡航速度降到 125，最快 AI 为 120；不踩油门仍可缓慢追车，但按油门会明显更有效。
- 新增键盘油门：按住 `↑`、`W` 或 `Space` 加速，松开后平滑回落。
- 油门速度余量固定约为 45；当前巡航速度由 Combo 决定。
- 速度目标：x0 125/170、x10 180/225、x20 220/265、x30 250/295、x50 280/325。
- 实际速度仍按每次超车连续提高；发动机基础转速则每 10 辆提高一级。
- x10、x20、x30 等转速变化使用约 0.3 秒平滑过渡。
- 油门在当前十辆区间内控制实时转速；按下响应快，松开回落稍慢。
- 超车提示音缩短到约 78ms，并在每十辆内部使用短音型循环。
- 浏览器窗口失去焦点时自动释放油门，避免卡在持续加速状态。
- V0.9.3 暂时不加入手机油门区，先验证核心玩法。

本文件记录每个原型版本的主要变化，便于 Git commit 后直接查看开发历史。

## V0.9.2 — Sound tuning

- 保留 V0.9.1 的内外车道真实路程修复，并将其作为后续版本基线。
- 降低主音量和发动机持续音量，避免长时间听觉疲劳。
- 发动机高频层从方波改为三角波，并降低高频占比。
- 增加随速度变化的轻微 RPM 脉冲，让持续声不再是单一长音。
- 发动机的音高、滤波和音量变化改得更平滑。
- 换道音效缩短、降调并降低音量。
- 超车音阶改为较低的六音循环，限制最高频率，长 Combo 不再刺耳。
- 动作音播放时发动机会短暂轻微 duck，提升层次而不是简单把音量做大。
- 最多保留 12 个短音 voice，防止高速连续事件造成音效堆积。

## V0.9.1 — Unified lane-speed fix

- 将 V0.8.2 的车道速度修复合并到带声音的主线版本，后续版本统一从本版继续。
- 修复内侧车道在进入弯道或经过直线/圆弧连接点时突然加速的问题。
- 不再只使用当前采样点的局部比例一次性推进，而是逐段消耗真实路面距离。
- 六条车道的同一 `speed` 现在都代表相同的实际线速度；外圈过弯时间更长，内圈路线更短，但车身在路面上的移动速度不会跳变。
- 变道中的小数车道也使用插值后的真实分段长度，避免快速换道时产生速度脉冲。
- 保留 V0.9 的发动机、换道和超车声音。


## V0.9 — Dynamic engine and action sounds

- 新增程序合成的连续发动机声，不依赖外部音频素材。
- 发动机基础频率、谐波、滤波器和音量会随红车实际速度平滑改变。
- 撞车停车时发动机迅速进入低沉弱音，恢复时跟随车速重新升高。
- 新增短促换道声；只有成功向相邻车道移动时播放。
- 新增超车提示音；Combo 对应不同音阶，使连续超车形成越来越明显的节奏。
- 浏览器使用标准 WebAudioContext，微信小游戏使用 `wx.createWebAudioContext`。
- 音频在第一次键盘或触屏操作时启动，以兼容浏览器和手机的自动播放限制。
- 保留 V0.8.1 的极简 HUD、长赛道、AI 安全距离和速度曲线。

## V0.8.1 — Minimal HUD and keyboard input fix

- 修复浏览器方向键偶发无响应：Canvas 可聚焦，并在 Canvas 与 Window 捕获阶段监听键盘事件。
- `← / →`、`A / D` 同时兼容 `event.key`、`event.code` 和旧式 `keyCode`。
- 删除顶部信息栏、速度、总超车、最高 Combo、Restart 按钮和底部可见方向按钮。
- 画面只在左上角保留一个紧凑 Combo 显示。
- 手机控制改为不可见的左右半屏触控区。
- 赛道由 6 段扩展为 8 段长直道，并向屏幕上下延伸，进一步增加高速连续超车空间。
- 保留 V0.8 的 18 辆 AI、速度联动安全区、分段加速和碰撞规则。
- 新增 `WECHAT_ONBOARDING.md`，记录从本地导入、真机预览、体验版本到审核发布的完整流程。

## V0.8 — Long Bay track polish

- 赛道重做为原创折叠式长环线：6 段长直道、5 个宽半圆弯和外侧回程线。
- 路线长度由 V0.7.x 的约 1400 轨迹单位增加到约 2350，适合在高速状态持续建立 Combo。
- 使用精确直线和圆弧构造赛道，取代手工 Catmull–Rom 控制点，减少曲率突变和不均匀车道。
- 赛道保持完全不交叉，不再使用任何桥面或遮罩。
- 调整六条车道宽度和车辆尺寸，确保长折返结构中仍有清晰间隔。
- 道路加入深色外沿、红白路肩、虚线分道和简化的微缩公园中岛。
- 红车采用更简洁的俯视轮廓和浅色赛车条，车模细节继续让位于高速可读性。
- 黑车从 14 辆增加到 18 辆，以维持更长赛道上的交通密度。
- 保留 V0.7.2 的速度联动 AI 安全区、预告变道、Combo、分段加速和碰撞规则。

## V0.7.2 — Speed-scaled AI safety zone

- 黑车在红车接近时禁止启动变道。
- 安全距离随红车速度增加：速度越高，黑车越早保持当前车道。
- 黑车在黄色方向灯预告期间，如果红车进入安全区，会取消本次变道。
- 红车刚超过黑车后，黑车也会在较短的后方保护距离内保持车道。
- 已经开始实际横移的黑车不会突然反向。
- 目的：减少高速阶段 AI 突然切线造成的“不公平碰撞”。

## V0.7.1 — Non-crossing loop

- 删除表现不稳定的伪 3D 交叉桥面和遮罩。
- 赛道改成不自相交的环形路线。
- 保留 AI 变道、Combo、分段加速和碰撞系统。

## V0.7 — AI lane changes

- 14 辆黑车开始有限度地变道。
- 黑车变道前显示黄色方向灯提示。
- 目标车道有车或玩家距离过近时取消变道。
- 同时进行预告或变道的 AI 数量受到限制。
- 曾尝试用 Canvas 绘制顺序表现上下层交叉口，但遮罩效果不稳定，后续在 V0.7.1 移除。

## V0.6.1 — Lower starting speed

- 红车基础速度从 165 降到 155。
- 保留前期快速加速、中后期持续但逐渐放缓的速度曲线。

## V0.6 — Staged acceleration curve

- 初始速度提高，并采用分段加速：
  - Combo 1–8：每次超车 +7
  - Combo 9–24：每次超车 +3.2
  - Combo 25–60：每次超车 +1.3
  - Combo 61 以后：每次超车 +0.55
- 目标是让玩家快速进入高速状态，同时可以在高速区间持续玩一段时间。

## V0.5 — Core combo loop

- 黑车增加到 14 辆。
- 超过黑车会增加 Combo 和总超车数。
- 每次超车会提升红车速度。
- 撞车后 Combo 清零，红车停车、闪烁并恢复。
- 加入最高 Combo 和重新开始按钮。
- 删除不再参与玩法的蓝色横线。

## V0.4 — Instant lane switching

- 取消只能在指定位置变道的限制。
- 方向键输入后立即开始换道。
- 红车换道动画缩短到约 0.05 秒。
- 允许高速连续输入，减少操作锁定。

## V0.3 — Cars centered in lanes

- 车辆改为行驶在两条分隔线之间，而不是压在线上。
- 加入键盘方向键和 A / D 控制。
- 曾加入变道输入缓存，但等待指定变道区造成明显延迟，后续在 V0.4 移除。
- 提高基础速度。

## V0.2 — First traffic and collision prototype

- 赛道扩展为 6 条车道。
- 加入左右按钮和 6 辆固定车道的慢速黑车。
- 加入基础碰撞规则：红车速度归零、闪烁、短暂无敌并恢复。
- 此时只能在指定变道区域进行换道。

## V0.1 — First moving prototype

- 一条原创俯视赛道。
- 一辆红车按固定速度自动绕圈。
- 没有 AI、碰撞、计分和操作。
- 目标是先验证微信小游戏 Canvas 工程和基本赛道运动。

## Current design decisions

- 使用原生 JavaScript + Canvas 2D，暂时不引入 Unity 或 Cocos。
- 红车只能逐条换道，但必须几乎即时响应。
- 黑车整体保持深色，红车使用醒目颜色。
- AI 行为必须可预判，不能依靠突然横移制造难度。
- 游戏的核心循环是：超车 → Combo → 加速 → 高速读车流 → 碰撞重置。
- UI、音效、震动和速度感将在核心车流稳定后重点制作，它们属于游戏体验的核心而非装饰。
